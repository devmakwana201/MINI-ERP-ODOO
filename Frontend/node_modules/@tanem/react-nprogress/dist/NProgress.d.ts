import type { FC, ReactElement } from 'react';
import type { Options } from '@tanem/react-nprogress/dist/types';
import { useNProgress } from '@tanem/react-nprogress/dist/useNProgress';
type Props = Options & {
    children: (renderProps: ReturnType<typeof useNProgress>) => ReactElement;
};
export declare const NProgress: FC<Props>;
export { };
